#ifndef UTILS_H
#define UTILS_H

// Function prototypes for utility functions
void log_message(const char *message);
int convert_speed_to_pwm(int speed);
void delay_ms(int milliseconds);

#endif // UTILS_H